pluginManagement {
    repositories {
        gradlePluginPortal()
    }
}

rootProject.name = 'sweep-server'

// Include local game-logic module from its actual location
include 'game-logic'
project(':game-logic').projectDir = new File('../Sweep-Base/game-logic')
